# Maintainer Backlog

## Top candidates

1. Push the local `ISSUE:11` follow-up that preserves transient zero uptime/traffic values around reconnect, then ask the reporter to verify the uptime graph.
2. Clean local scratch artifacts before any commit or release branch.
3. Consider adding an issue template that asks for router model, firmware version, Home Assistant version, and sanitized debug logs.
